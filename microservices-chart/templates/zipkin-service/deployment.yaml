apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.zipkin.name }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.zipkin.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.zipkin.name }}
    spec:
      containers:
        - name: {{ .Values.zipkin.name }}
          image: {{ .Values.zipkin.image }}
          ports:
            - containerPort: {{ .Values.zipkin.port }}
