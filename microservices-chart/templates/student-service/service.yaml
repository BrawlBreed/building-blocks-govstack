apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.studentService.serviceName }}
spec:
  selector:
    app: {{ .Values.studentService.name }}
  ports:
    - port: {{ .Values.studentService.port }}
      targetPort: {{ .Values.studentService.port }}
  type: ClusterIP
